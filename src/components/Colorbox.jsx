import React from "react";

const Colorbox = ({ color }) => {
  return (
    <div
      style={{
        backgroundColor:  color,
        minWidth: "15rem",
        minHeight: "15rem",
      }}
    >
      
    </div>
  );
};

export default Colorbox;
